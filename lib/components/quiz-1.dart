
import 'package:flutter/material.dart';

class Quiz1 extends StatelessWidget{
@override 
Widget build(BuildContext context){
  return Scaffold(
    appBar: AppBar(title: Text("welcome to quiz-1")),
    body: Container(
      height: 100,
      width: 100,
      color: Colors.blue,
    ),
  );

  
}
}